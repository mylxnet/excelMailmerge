# Excel 邮件合并工具 项目设计方案

> 版本：v1.1.2  
> 编制日期：2026-08-24  
> 最近更新：2026-09-17
> 作者：by Mr lin
> 状态：已发布（v1.1.2）

---

## 一、开发目的

### 1.1 项目背景

在企业办公场景中，常需要根据 Excel 数据源批量生成结构化的电子表格文件（如工资单、成绩单、收据、合同附件等）。传统"邮件合并"功能主要面向 Word 文档，对 Excel 模板的支持较弱，且操作繁琐、无法保留模板原有的样式与公式计算结果。

本工具旨在填补这一空白，提供一款 Windows 桌面应用，能够：

- 以 Excel 数据源（支持 Office Excel 的 `.xlsx` / `.xls` 与 WPS 电子表格的 `.et`，后两者自动转换为 `.xlsx`）为输入
- 以用户提供的 Excel 模板（含 `{列名}` 占位符）为样式依据
- 为数据源中的每一行数据生成一个或多个 Sheet，输出新的 Excel 文件

### 1.2 解决的核心问题

| 痛点 | 本工具解决方案 |
|------|----------------|
| 邮件合并主要支持 Word，Excel 模板支持弱 | 专为 Excel 模板设计，原生支持 `{列名}` 占位符 |
| 占位符匹配易错、难排查 | 占位符匹配预检 + 缺失列警告 + 试生成预览 |
| 模板样式易被破坏 | 结构锁定，仅允许修改单元格文本内容（占位符） |
| 输出文件易覆盖 | 自动新建时间戳文件夹 `输出结果yyyyMMdd_HHmmss` |
| 公式处理不当导致结果错误 | 公式自动转为计算值，不保留原公式 |
| 命名冲突 | 主命名列 + 次命名列 + 自动追加序号 |
| 数据源格式不统一（Office `.xls` / WPS `.et`） | 内置格式转换器，自动转为 `.xlsx` 后统一处理 |
| 单文件模式数据量过大导致卡死 | 模板仅打开一次 + 行探查边界保护 + Sheet 数上限校验 |
| 重复启动产生多实例 / 残留进程占用文件 | 启动即清理残留进程 + 单实例互斥锁 |
| 商用组件授权风险 | 采用 ClosedXML（MIT 协议），完全免费商用 |

### 1.3 目标用户

- 企业 HR、财务、行政人员
- 需要批量生成结构化 Excel 报表的工作人员
- 熟悉 Office / WPS 电子表格操作的非技术用户

---

## 二、开发语言与技术栈

### 2.1 技术选型

| 层次 | 技术选型 | 说明 |
|------|----------|------|
| 运行平台 | Windows 桌面 | .NET 8 运行时 |
| 开发框架 | WPF (Windows Presentation Foundation) | XAML + 数据绑定，界面表现力强 |
| 编程语言 | C# 12 / .NET 8 | 目标框架 `net8.0-windows` |
| 架构模式 | MVVM | `ObservableObject` + `RelayCommand` 实现 |
| Excel 操作 | ClosedXML | MIT 协议，商用合规，支持 `.xlsx` |
| 数据序列化 | Newtonsoft.Json | 配置持久化 |
| 异步模型 | `async/await` + `CancellationToken` | 生成任务可取消 |
| 格式兼容 | COM 互操作（`et.Application` → `Ket.Application` → `Excel.Application`） | `.xls` / `.et` 自动转换为 `.xlsx` |
| 进程管理 | 命名 `Mutex` + 命名管道 | 单实例运行、激活已有窗口 |

### 2.2 选型理由

**为何选择 ClosedXML 而非 EPPlus？**
- EPPlus 7.x 起采用 PolyForm Noncommercial 协议，商用需购买授权
- ClosedXML 采用 MIT 协议，完全免费商用，满足企业合规要求
- ClosedXML API 简洁，对样式保留、公式处理支持良好

**为何选择 WPF 而非 WinForms？**
- WPF 的 XAML + 数据绑定机制更适合实现递进式向导界面
- 样式与逻辑分离，便于实现"暖琥珀商务"风格设计
- MVVM 模式提升代码可维护性

### 2.3 项目结构

```
ExcelMailMerge/                       # 主程序（net8.0-windows）
├── ExcelMailMerge.csproj            # 项目文件（含 ApplicationIcon=app.ico）
├── nuget.config / app.ico           # 包源配置 / 应用图标
├── App.xaml / App.xaml.cs           # 应用入口（单实例检测 + 残留进程清理 + 崩溃日志）
├── MainWindow.xaml(.cs)             # 主界面（递进式向导）
├── ServiceLocator.cs                # 服务定位
├── Models/                          # 数据模型
│   ├── AppSettings.cs               # 配置模型（持久化）
│   ├── DataSourceModels.cs          # 列信息 / 数据行 / 解析后的数据源
│   ├── TemplateAndValidationModels.cs # 模板占位符 / 扫描结果 / 校验结果
│   ├── GenerationModels.cs          # 生成进度（INotifyPropertyChanged）/ 输出模式
│   └── WizardModels.cs              # 向导步骤模型
├── ViewModels/                      # 视图模型
│   └── MainViewModel.cs             # 主视图模型（业务逻辑）
├── Views/                           # 窗口视图
│   └── TemplateEditorWindow.xaml(.cs)  # 模板编辑器（外部 Excel/WPS 打开 + 自动重载）
├── Services/                        # 服务层
│   ├── DataSourceService.cs         # 数据源解析
│   ├── TemplateService.cs           # 模板扫描
│   ├── ValidationService.cs         # 预校验（含 Sheet 数上限校验）
│   ├── NamingService.cs             # 命名规则
│   ├── GenerationEngine.cs          # 生成引擎
│   ├── FileFormatConverter.cs       # .xls/.et → .xlsx（COM 互操作）
│   └── SettingsService.cs           # 配置读写
├── Helpers/                         # 辅助类
│   ├── ObservableObject.cs          # MVVM 基类
│   ├── RelayCommand.cs              # 同步命令
│   └── AsyncRelayCommand.cs         # 异步命令
└── Converters/                      # 值转换器
    └── CommonConverters.cs          # 含 InverseBoolConverter 等

ExcelMailMerge.Tests/                # 单元测试项目（xUnit）
└── GenerationEngineTests.cs         # 生成引擎测试（单文件/多文件、进度、取消、公式转值等）
```

---

## 三、主要功能

### 3.1 功能总览

应用采用**四步递进式向导**设计：**数据源 → 模板 → 预览 → 生成**，前一阶段校验通过后下一阶段才解锁。

```
┌──────────┐     ┌──────────┐     ┌──────────┐     ┌──────────┐
│ ① 数据源  │ →→→ │ ② 模板    │ →→→ │ ③ 预览    │ →→→ │ ④ 生成    │
│ 上传校验  │     │ 编辑占位符 │     │ 试生成确认 │     │ 设置全部生成│
└──────────┘     └──────────┘     └──────────┘     └──────────┘
```

### 3.2 阶段一：数据源

**功能目标**：上传数据源文件，校验规范性，引导用户修正问题。

| 功能项 | 说明 |
|--------|------|
| 上传数据源 | 支持 `.xlsx` / `.xls` / `.et`（WPS），支持拖放上传；非 `.xlsx` 自动转换 |
| 选择 Sheet | 多 Sheet 时下拉选择 |
| 标题行号 | 默认首行非空自动识别，可手动调整 |
| 数据预览 | 显示前 5 行数据，展示识别到的列名 |
| 自动校验 | 合并单元格检测、标题识别、数据连续性、长数字检测 |
| 校验结果 | 通过/警告/错误分级展示，错误项引导用户修正 |

**校验规则：**

- 数据源**禁止合并单元格**，检测到则报错
- 默认首个非空行为标题行，数据从标题行+1 开始，至首个空行停止
- 长数字（>11 位）自动标记为文本类型
- 空值在生成时替换为空字符串

**校验通过后**：②模板、③预览、④生成 阶段解锁可用。

### 3.3 阶段二：模板

**功能目标**：上传模板，把数据源列名以 `{列名}` 占位符写入模板（编辑动作交给 Excel/WPS 完成）。

> **实现方案（方案 B）**：早期曾尝试用 DataGrid 高仿渲染 Excel 网格（写了 600+ 行后放弃）。现改为「外部 Excel/WPS 打开 + 剪贴板复制占位符 + 文件变更自动重载」，格式 100% 还原，代码量约 200 行。

| 功能项 | 说明 |
|--------|------|
| 上传模板 | 用户上传已有模板文件（不支持新建空白模板），支持 `.xlsx` / `.xls` / `.et` |
| 数据源列名面板 | 左栏列出数据源所有列名，支持搜索过滤 |
| 复制占位符 | 点击列名（或点 📋 按钮）→ 复制 `{列名}` 到剪贴板 |
| 用 Excel/WPS 打开 | 一键调用系统默认程序打开模板，用户在 Excel 中 Ctrl+V 粘贴 |
| 极简预览 | 右栏 DataGrid 只显示文本与占位符匹配情况（**不显示行号列**），样式以 Excel 为准 |
| 自动重载 | `FileSystemWatcher` 监听模板保存，编辑完 Ctrl+S 后自动刷新预览 |
| 结构保护 | 模板结构（行高列宽、合并单元格）由用户在 Excel 中自行控制，本工具不修改 |

**占位符规则：**

- 语法：`{列名}`，大括号包裹数据源标题行列名
- 大小写敏感
- 自动 Trim 标题与占位符前后空格
- 缺失列：显示警告并替换为空字符串
- 空值：替换为空字符串
- 标题列名禁止包含大括号

**编辑操作说明：**

- 编辑动作在 Excel/WPS 中完成：双击编辑、删除、剪切、复制、粘贴、撤销/重做均由 Excel 提供
- 本工具不再提供"智能覆盖/追加菜单"，占位符插入位置由用户在 Excel 中自行决定
- 本工具不修改模板结构（行高列宽 / 合并单元格 / 插入行列均不在应用内操作）

### 3.4 阶段三：预览

**功能目标**：试生成第 1 条数据，让用户确认填充效果后再全部生成。

| 功能项 | 说明 |
|--------|------|
| 试生成 | 用第 1 行数据填充模板，生成临时预览文件（`预览_第1条.xlsx`） |
| 文件预览 | 内嵌 Excel 网格预览，支持 Sheet Tab 切换 |
| 填充效果检查 | 占位符替换、数据类型、长数字转文本、公式转值 |
| 样式保留检查 | 单元格样式、字体字号、合并单元格、行高列宽 |
| Sheet 处理检查 | 各 Sheet 占位符是否均已替换 |
| 用 Excel 打开 | 调用系统默认程序打开临时文件 |
| 重新试生成 | 重新生成预览 |
| 返回模板修改 | 回到阶段二调整 |
| 确认效果 | 进入阶段四，临时文件在全部生成时清除 |

**临时文件规则：**

- 预览文件为临时文件，不占用正式输出目录
- 进入生成阶段并执行全部生成时，自动清除临时预览文件

### 3.5 阶段四：生成

**功能目标**：配置输出参数，批量生成所有文件。

#### 3.5.1 基本设置

| 设置项 | 说明 |
|--------|------|
| 保存文件夹 | 自动新建时间戳文件夹 `输出结果yyyyMMdd_HHmmss`，永不覆盖 |
| 保存形式 | 多文件（每行一个文件）/ 单文件（全部行一个文件多 Sheet） |
| 命名规则 | 主命名列 + 次命名列（可选）+ 分隔符 + 重名处理，四项排同一行 |
| 文件名预览 | 实时预览生成文件名，含重名场景示例 |

**命名规则细节：**

- 主命名列（必选）：下拉选择数据源列
- 次命名列（可选）：用于重名区分，下拉选择
- 分隔符：自定义（默认 `_`，最多 3 字符）
- 重名处理：自动追加序号（如 `张三_001.xlsx`、`张三_002.xlsx`）

**保存形式说明：**

- **多文件**：每行数据生成一个独立文件，文件内包含模板所有 Sheet
- **单文件**：全部行数据生成到一个文件，每行对应一个 Sheet（多 Sheet 模板与此模式冲突时提示用户切换）

#### 3.5.2 高级设置

| 设置项 | 默认值 | 说明 |
|--------|--------|------|
| 保留 DateTime / 数值类型 | ✓ | 写入时保留原始数据类型 |
| 长数字（>11位）自动转文本 | ✓ | 加单引号前缀，防止科学计数法 |
| 空值替换为空字符串 | ✓ | null 值写入空字符串 |
| 公式替换为计算值 | ✓ | 不保留原公式 |
| 生成后自动打开输出目录 | ✓ | 生成完成调用资源管理器打开 |
| 记住本次配置 | ✓ | 命名规则 / 高级设置下次启动恢复；**数据源与模板路径不再记忆** |
| 缺失列显示警告并留空 | ✓ | 缺失列警告并替换为空 |
| 占位符大小写敏感 | ✓ | 大小写敏感匹配 |

#### 3.5.3 生成执行

| 功能项 | 说明 |
|--------|------|
| 预校验状态 | 校验通过后才允许生成 |
| 进度条 | 显示当前进度（如 45/128）与百分比 |
| 暂停/取消 | 支持中途暂停与取消（`CancellationToken`） |
| Sheet 数上限校验 | 单文件模式下若 `模板Sheet数 × 数据行数 > 200`，校验阶段报错并阻断生成 |
| 生成中锁定窗口 | 生成过程中底部"关闭"按钮禁用，避免误操作中断 |
| 后台线程执行 | 生成逻辑整体在 `Task.Run` 线程池执行，界面不阻塞、进度与日志实时刷新 |
| 实时日志 | 成功（绿）/警告（黄）/错误（红）分级日志 |
| 打开输出目录 | 快捷打开输出文件夹 |
| 清除临时文件 | 全部生成时清除预览阶段的临时文件 |

### 3.6 配置持久化

命名规则、高级设置等保存到本地 JSON 文件 `%LOCALAPPDATA%\ExcelMailMerge\settings.json`，下次启动自动恢复。

**不持久化的项**：数据源路径、模板路径 —— 每次启动均为空，避免误用上次的旧文件；且数据源校验失败时会清空其路径。

### 3.7 单实例与进程管理

| 机制 | 实现位置 | 说明 |
|------|----------|------|
| 残留进程清理 | `App.xaml.cs::KillRunningInstances` | 启动前结束所有同名进程（`Kill(entireProcessTree: true)` + `WaitForExit(5000)`），释放被占用的文件锁 |
| 单实例互斥锁 | 命名 `Mutex`：`ExcelMailMerge_SingleInstance_Mutex` | 同一时间只允许一个实例运行 |
| 互斥锁重试获取 | `TryAcquireMutex(20, 200)` | 强杀旧进程后命名内核对象释放存在延迟，故重试 20 次 × 200ms |
| 激活已有窗口 | 命名管道：`ExcelMailMerge_ActivatePipe` | 第二个实例唤起已有实例窗口后自身退出 |

---

## 四、界面设计

### 4.1 设计风格

**暖琥珀商务风格**

- 主色：深墨蓝 `#1a2540`
- 强调色：琥珀金 `#d4a256`
- 成功色：翠绿 `#10b981`
- 警告色：琥珀黄 `#f6ad55`
- 错误色：红色 `#e53e3e`
- 背景色：浅灰 `#f5f6fa`
- 标题字体：Noto Serif SC（衬线）
- 正文字体：Noto Sans SC（无衬线）
- 等宽字体：Consolas（代码/文件名）

### 4.2 整体布局

```
┌─────────────────────────────────────────────────┐
│ 标题栏（深墨蓝 + 琥珀金 Logo + 应用名）           │
├─────────────────────────────────────────────────┤
│ 步骤导航（四步递进，已完成绿色✓，当前琥珀脉动）   │
│  ①数据源 → ②模板 → ③预览 → ④生成                │
├─────────────────────────────────────────────────┤
│                                                  │
│              当前阶段内容区                       │
│                                                  │
├─────────────────────────────────────────────────┤
│ 底部操作栏（上一步 / 下一步 / 生成）              │
└─────────────────────────────────────────────────┘
```

**标题栏**：应用名 + 版本号 + 署名，形如「Excel 邮件合并工具&nbsp;&nbsp;v1.1.2&nbsp;&nbsp;「by Mr lin」」。

**滚动策略**：主界面外层 `ScrollViewer` 设置为 `VerticalScrollBarVisibility="Disabled"`（不出现整页滚动条），内容超出时由内部控件（数据预览表、预览 DataGrid）各自的滚动条承担。

### 4.3 步骤导航状态

| 状态 | 视觉表现 |
|------|----------|
| 已完成 | 绿色填充圆形 + 白色 ✓ + 绿色标签 |
| 当前 | 琥珀色脉动圆形 + 数字 + 琥珀色加粗标签 |
| 未激活 | 灰色圆形 + 数字 + 灰色标签 |

进度条：已完成部分绿色，当前过渡到琥珀色，未激活部分灰色。

### 4.4 各阶段界面要点

#### ① 数据源界面
- 上传区（拖放支持，已上传显示文件信息）
- 路径框 + 按钮组：**「📂 浏览…」在左、「🔄 刷新Sheet」在右**
- Sheet 选择 + 标题行号配置
- 前 5 行数据预览表（表格自带滚动条）
- 自动校验结果面板（通过/警告/错误分级）

#### ② 模板界面
- 上传区（拖放支持）+ 路径框 + 按钮组：**「📂 浏览…」在左、「✨ 编辑模板」在右**
- 点「✨ 编辑模板」打开**独立的模板编辑器窗口**（见下）

**模板编辑器窗口（方案 B，左右两栏）：**
- **左栏**：数据源列名列表（带搜索框；点击列名或 📋 按钮即复制 `{列名}` 到剪贴板）
- **右栏**：顶部「📄 用 Excel/WPS 打开模板」大按钮 + 模板路径；下方极简预览 DataGrid（`HeadersVisibility="Column"`，**无左侧行号列**，仅显示文本与占位符）；「🔄 重新加载」按钮
- **底部**：「❌ 取消并关闭」/「✅ 模板编辑完成，返回主界面」
- 样式与格式请在 Excel/WPS 中查看，本窗口不做高仿渲染

#### ③ 预览界面
- 说明栏（试生成说明 + 重新试生成按钮）
- 数据来源行（数据源/模板/数据行/临时文件名）
- 左右双栏：左侧文件预览（临时文件标签）、右侧填充/样式/Sheet 检查
- 深蓝底确认栏（重新试生成 / 返回模板修改 / 确认效果进入生成）

#### ④ 生成界面
- 基本设置卡片（保存文件夹 + 保存形式 + 命名规则四项同行 + 文件名预览）
- 高级设置卡片（可展开/收起，数据写入 + 生成行为）
- 预校验状态条
- 进度条 + 实时日志（左右双栏）
- 生成过程中：底部「关闭」按钮**禁用**，避免误操作中断生成

---

## 五、代码审计

### 5.1 架构规范

- **MVVM 分层**：View（XAML）↔ ViewModel（业务逻辑）↔ Model（数据）↔ Service（服务）
- **服务定位**：通过 `ServiceLocator` 统一管理服务实例，避免硬依赖
- **单一职责**：每个 Service 只负责一个领域（数据源/模板/校验/生成）

### 5.2 编码规范

| 规范项 | 要求 |
|--------|------|
| 命名 | PascalCase（公开成员）、_camelCase（私有字段） |
| 注释 | 中文注释，公开成员需 XML 文档注释 |
| 异步 | 异步方法加 `Async` 后缀，使用 `CancellationToken` |
| 空值 | 启用可空引用类型（NRT），显式标注 `?` |
| 异常 | 不吞异常，边界捕获并记录日志 |

### 5.3 关键代码审查点

| 审查点 | 要求 |
|--------|------|
| 资源释放 | `ClosedXML` 的 `XLWorkbook` 需 `using` 或显式 `Dispose` |
| 异步取消 | 生成任务支持 `CancellationToken`，UI 可触发取消 |
| 路径安全 | 输出路径拼接使用 `Path.Combine`，避免路径穿越 |
| 文件覆盖 | 严格使用时间戳文件夹，禁止覆盖已有文件 |
| 公式转值 | 生成时读取公式计算结果写入，不保留 `Formula` 属性 |
| 样式保留 | 编辑模板时仅改 `CellValue`，不动 `Style` 相关属性 |
| 长数字 | 检测数字长度 >11 位时，以文本写入并加 `'` 前缀 |
| 合并单元格 | 数据源检测到合并单元格直接报错，不继续处理 |
| 循环边界 | 循环边界必须在循环外捕获为局部变量，**禁止在循环体内修改**（否则边界随循环推移，会一直跑到 Excel 最大行 1048576） |
| 行高判定 | 判断"是否设置了自定义行高"必须与默认行高比较（`Math.Abs(h - 默认行高) > 0.01`），不能用 `h > 0` |
| 绑定通知 | WPF 绑定的模型类必须实现 `INotifyPropertyChanged`；计算属性需在依赖字段变化时**联动通知** |
| 集合通知 | 绑定到日志区/列表的集合必须用 `ObservableCollection<T>`，不能用 `List<T>` |
| 进程互斥 | 用命名 `Mutex` 判断"是否已有实例"时，进程被强杀后内核对象释放有延迟，必须做重试 |
| 线程隔离 | CPU 密集的 ClosedXML 操作必须放在 `Task.Run` 线程池执行，避免阻塞 UI 线程 |

### 5.4 数据安全

- 不上传任何数据到网络，全部本地处理
- 配置文件仅存储路径与设置，不含敏感数据
- 临时预览文件在生成完成后清除

---

## 六、打包分发

### 6.1 打包方式

采用 .NET 8 的**独立部署（Self-contained）**发布，目标用户机器无需安装 .NET 运行时。

```bash
dotnet publish -c Release -r win-x64 --self-contained true -p:PublishSingleFile=true -p:IncludeNativeLibrariesForSelfExtract=true -p:EnableCompressionInSingleFile=true -o ./publish
```

> ⚠️ **不可**在 csproj 中添加 `<InvariantGlobalization>true</InvariantGlobalization>`：WPF 不支持 invariant globalization 模式，会导致 Release 版启动即崩溃。
>
> 发布三个关键参数缺一不可：`PublishSingleFile` + `IncludeNativeLibrariesForSelfExtract` + `EnableCompressionInSingleFile`（后者使体积从 ~100MB 压到 ~71MB）。

### 6.2 发布产物

| 产物 | 说明 |
|------|------|
| `publish/ExcelMailMerge.exe` | 单文件可执行程序（含运行时 + 应用图标） |
| 体积 | 实测约 **71 MB**（含 .NET 运行时 + ClosedXML，已启用单文件压缩） |
| 启动开销 | 首次启动约 1–2 秒（自解压），运行时常驻内存 250 MB+ |

### 6.3 分发方式

- 将 `ExcelMailMerge.exe` 直接分发给用户
- 用户双击即可运行，无需安装
- 可选：打包为 ZIP 或制作安装包（Inno Setup）

### 6.4 依赖清单

| 依赖 | 版本 | 协议 | 用途 |
|------|------|------|------|
| ClosedXML | 0.102.0 | MIT | Excel 读写 |
| DocumentFormat.OpenXml | 3.1.1 | MIT | ClosedXML 底层依赖（传递引入） |
| Newtonsoft.Json | 13.0.3 | MIT | 配置序列化 |
| .NET 8 运行时 | 8.0.x | MIT | 运行环境（独立部署内含） |
| COM 互操作（WPS / Excel） | 系统自带 | — | 将 `.xls` / `.et` 转换为 `.xlsx`；**前提：用户机器已安装 WPS 或 Microsoft Excel** |

---

## 七、容错纠错

### 7.1 错误处理原则

- **失败时提示用户修改正确后再执行，不跳过**
- **校验类错误在校验阶段就提示**：校验不通过则「生成」按钮不可用，而不是等用户点了生成才弹窗
- 预校验必须在生成前完成，错误项列出并标注行号
- 所有错误均给出明确的修正指引

### 7.2 容错场景与处理

| 场景 | 处理方式 |
|------|----------|
| 数据源含合并单元格 | 报错，列出位置，要求用户修正后重新上传 |
| 数据源标题行有空列 | 警告，建议补全列名 |
| 模板占位符未匹配到列 | 警告，列出占位符列表，生成时替换为空 |
| 命名规则冲突（多 Sheet + 单文件） | 冲突警告，引导用户切换为"多文件"模式 |
| 输出文件名重名 | 自动追加序号（如 `张三_001`） |
| 生成中异常 | 暂停生成，日志记录错误行，提示用户修正后重试 |
| 长数字被识别为科学计数法 | 自动以文本写入并加单引号前缀 |
| 公式计算失败 | 记录警告，保留原值或置空，不中断生成 |
| 配置文件损坏 | 捕获异常，回退默认配置，不崩溃 |
| 文件被占用无法写入 | 提示用户关闭已打开的文件后重试 |
| 单文件模式 Sheet 总数超限 | 校验阶段报错（`模板Sheet数 × 数据行数 > 200`），提示改用多文件模式或拆分数据源，并阻断生成 |
| `.xls` / `.et` 转换失败 | 捕获异常并提示"请确认已安装 WPS 或 Microsoft Excel" |
| 启动时存在残留进程 | 自动结束所有同名进程后启动，释放被占用的文件锁 |
| 重复启动应用 | 唤起已有窗口后当前实例退出（单实例锁 + 命名管道） |
| 生成过程界面假死 | 生成逻辑在 `Task.Run` 线程池执行，UI 保持响应并实时刷新进度与日志 |
| 单文件生成每行耗时异常（如 100 秒/行） | 已定位为行探查循环边界被循环体修改，修复后正常 |

### 7.3 校验机制

**三层校验：**

1. **数据源校验**（阶段一）：合并单元格、标题识别、数据连续性
2. **占位符匹配校验**（阶段二）：模板占位符与数据源列名匹配
3. **生成前预校验**（阶段四）：数据源、模板、命名规则、冲突检测综合校验

### 7.4 日志记录

- 运行日志实时显示在生成阶段日志区
- 日志分级：成功（绿）/ 警告（黄）/ 错误（红）/ 信息（灰）
- 每条日志含时间戳
- 关键错误可导出日志文件供排查

---

## 八、使用方法

### 8.1 快速入门

1. **启动程序**：双击 `ExcelMailMerge.exe`

2. **第一步：数据源**
   - 点击「📂 浏览…」或拖放文件到上传区（支持 `.xlsx` / `.xls` / `.et`，非 .xlsx 自动转换）
   - 选择 Sheet（如多 Sheet），或点「🔄 刷新Sheet」重新读取
   - 确认标题行号（默认首行）
   - 查看前 5 行数据预览
   - 等待自动校验通过（绿色提示）→ 点击"下一步"

3. **第二步：模板**
   - 点击「📂 浏览…」选择模板文件（支持 `.xlsx` / `.xls` / `.et`）
   - 点击「✨ 编辑模板」打开模板编辑器窗口
   - 在编辑器中点击「📄 用 Excel/WPS 打开模板」，用 Excel/WPS 打开模板文件
   - 在编辑器左栏点击列名（或点 📋 按钮）复制 `{列名}` 到剪贴板
   - 回到 Excel，选中目标单元格 Ctrl+V 粘贴；重复插入所有占位符
   - 在 Excel 中 Ctrl+S 保存，编辑器窗口会自动重载预览（确认占位符均已匹配）
   - 点「✅ 模板编辑完成，返回主界面」→ 点击"下一步"

4. **第三步：预览**
   - 系统自动试生成第 1 条数据的临时预览文件
   - 查看 Excel 网格预览：占位符是否正确替换、数据类型是否正确、样式是否保留
   - 查看右侧检查项：填充效果、样式保留、Sheet 处理
   - 如需调整，点击"返回模板修改"
   - 确认无误后点击"确认效果 · 进入生成"

5. **第四步：生成**
   - **基本设置**：
     - 确认保存文件夹（自动时间戳命名）
     - 选择保存形式（多文件/单文件）
     - 设置命名规则（主命名列 + 次命名列 + 分隔符 + 重名处理）
     - 查看文件名预览
   - **高级设置**（可展开）：按需调整数据写入与生成行为选项
   - 确认预校验通过（单文件模式下若 `模板Sheet数 × 数据行数 > 200`，会提示改用多文件模式或拆分数据源）
   - 点击"立即生成"
   - 查看进度条与实时日志（生成过程中底部「关闭」按钮禁用，请等待完成）
   - 生成完成后自动打开输出目录

### 8.2 占位符语法示例

模板中写入：

```
员工工资单
姓名：{姓名}
工号：{工号}
部门：{部门}
金额：{金额}
```

数据源：

| 姓名 | 工号 | 部门 | 金额 |
|------|------|------|------|
| 张三 | EMP001 | 销售部 | 5800 |

生成结果：

```
员工工资单
姓名：张三
工号：EMP001
部门：销售部
金额：5800
```

### 8.3 命名规则示例

| 主命名列 | 次命名列 | 分隔符 | 生成文件名 |
|----------|----------|--------|------------|
| 姓名 | — | _ | 张三.xlsx、李四.xlsx |
| 姓名 | 工号 | _ | 张三_EMP001.xlsx |
| 部门 | 姓名 | - | 销售部-张三.xlsx |

重名场景（主命名列有重名"王五"）：

```
王五.xlsx
王五_001.xlsx
王五_002.xlsx
```

### 8.4 输出目录结构

```
输出结果20260824_215942/
├── 张三.xlsx
├── 李四.xlsx
├── 王五.xlsx
├── 王五_001.xlsx
├── 赵六.xlsx
└── ...
```

### 8.5 常见问题

| 问题 | 解决方案 |
|------|----------|
| 数据源有合并单元格报错 | 取消合并单元格后重新上传 |
| 占位符未替换 | 检查占位符列名与数据源标题是否大小写一致 |
| 长数字显示为科学计数法 | 已自动转文本，如仍异常检查高级设置 |
| 生成失败 | 查看日志区错误信息，修正后重试 |
| 文件无法写入 | 关闭已打开的输出文件后重试 |
| 选了 `.xls` / `.et` 却提示转换失败 | 确认本机已安装 WPS 或 Microsoft Excel |
| 单文件模式提示 Sheet 总数超限 | 改用「多文件」模式，或拆分数据源分次生成 |
| 双击图标后出现多个窗口 | 应用为单实例运行，重复启动会唤起已有窗口，不会真正多开 |
| 生成时窗口"没反应"，但进度条在走 | 属正常，生成在线程池执行；生成期间关闭按钮已禁用，请等待完成 |
| 上次的数据源 / 模板路径没有被记住 | 属预期设计：路径不再持久化，每次启动需重新选择 |

---

## 九、开发计划

### 9.1 模块开发顺序

1. **基础框架**：项目创建、MVVM 基类、服务定位
2. **数据源服务**：Excel 解析、Sheet 选择、标题识别、前5行预览、`.xls` / `.et` 转换
3. **校验服务**：合并单元格、标题、数据连续性、长数字检测、单文件 Sheet 数上限
4. **模板服务**：模板扫描、占位符提取
5. **模板编辑器**：方案 B —— 列名复制剪贴板 + 外部 Excel/WPS 打开 + `FileSystemWatcher` 自动重载
6. **生成引擎**：占位符替换、公式转值、样式保留、长数字处理
7. **预览功能**：试生成第1条、临时文件、效果检查
8. **生成界面**：基本设置、高级设置、命名规则、进度日志
9. **配置持久化**：JSON 读写、自动恢复
10. **递进式向导**：四步导航、阶段解锁
11. **打包发布**：独立部署、单文件发布

以上模块已在 v1.0.0 全部完成，v1.1.x 为在此基础上迭代（详见 9.3 版本变更记录）。

### 9.2 待确认事项

- [x] 界面设计图最终确认
- [x] 配置文件存储位置（应用目录 or 用户目录）→ 定为 `%LOCALAPPDATA%\ExcelMailMerge\settings.json`
- [x] 是否支持 `.xls`（老格式）或仅 `.xlsx` → 已支持 `.xls` 与 `.et`（自动转换为 `.xlsx`）
- [x] csproj 版本号与界面版本号统一 → 已统一为 `1.1.2`
- [ ] 日志是否需要导出文件功能

### 9.3 版本变更记录

#### v1.1.2（2026-09-17）

| # | 变更 | 类型 |
|---|------|------|
| 1 | 修复行探查死循环（循环边界变量被循环体修改），单文件生成每行耗时从 100+ 秒恢复正常 | 修复 |
| 2 | 修复"自定义行高"判定（与默认行高比较，而非 `> 0`） | 修复 |
| 3 | 单文件模式性能优化：模板只打开一次 | 优化 |
| 4 | 新增单文件模式 Sheet 数上限校验（`模板Sheet数 × 数据行数 > 200` 报错阻断） | 功能 |
| 5 | 实时日志修复：日志集合由 `List<string>` 改为 `ObservableCollection<string>` | 修复 |
| 6 | 进度条修复：进度模型实现 `INotifyPropertyChanged`，并对计算属性联动通知 | 修复 |
| 7 | 启动时自动清理残留进程（`KillRunningInstances` + `WaitForExit(5000)`） | 功能 |
| 8 | 单实例锁改为重试获取（`TryAcquireMutex(20, 200)`），修复"连开两次后进程数为 0" | 修复 |
| 9 | 生成过程中禁用底部「关闭」按钮 | 功能 |
| 10 | 数据源区按钮顺序对调（「浏览…」在左、「刷新Sheet」在右） | 调整 |
| 11 | 新增测试报告 `TEST_REPORT.md`；csproj 版本号统一为 1.1.2 | 文档 |

#### v1.1.1（2026-09-17）

| # | 变更 | 类型 |
|---|------|------|
| 1 | 标题栏在署名前加版本号（`v1.1.1 「by Mr lin」`） | 调整 |

#### v1.1.0（2026-09-17）

| # | 变更 | 类型 |
|---|------|------|
| 1 | 新增 `.xls` / `.et` 支持：`Services/FileFormatConverter.cs` 通过 COM 互操作自动转为 `.xlsx` | 功能 |
| 2 | 新增应用图标 `app.ico`（用 csproj `<ApplicationIcon>`，**不写 XAML `Icon`**） | 功能 |
| 3 | 新增单实例运行（命名 `Mutex` + 命名管道唤起已有窗口） | 功能 |
| 4 | 修复输出列宽与模板不一致（列宽复制不再跳列，并向后探查 30 列） | 修复 |
| 5 | 模板编辑器预览去掉左侧行号列（`HeadersVisibility="Column"`） | 调整 |
| 6 | 主界面不出现整页滚动条，滚动交由内部控件承担 | 调整 |
| 7 | 数据源 / 模板路径不再持久化；数据源校验失败时清空其路径 | 调整 |
| 8 | 校验失败不在选择模板时弹窗，改在生成界面校验时提示 | 调整 |
| 9 | 关闭按钮样式转换器 `InverseBoolConverter` | 功能 |
| 10 | 标题栏加入署名「by Mr lin」 | 调整 |

#### v1.0.0（2026-08-27）

首个正式版本：四步递进向导（数据源 → 模板 → 预览 → 生成）、数据源校验、`{列名}` 占位符填充、多文件 / 单文件两种输出模式、命名规则、self-contained 单文件发布。

---

## 十、附录

### 10.1 色彩规范

| 用途 | 色值 |
|------|------|
| 主色（深墨蓝） | #1a2540 |
| 强调色（琥珀金） | #d4a256 |
| 成功色（翠绿） | #10b981 |
| 警告色（琥珀黄） | #f6ad55 |
| 错误色（红） | #e53e3e |
| 背景色（浅灰） | #f5f6fa |
| 文字主色 | #1a2540 |
| 文字次要 | #4a5568 |
| 文字辅助 | #9ca3af |

### 10.2 字体规范

| 用途 | 字体 |
|------|------|
| 标题（衬线） | Noto Serif SC |
| 正文（无衬线） | Noto Sans SC |
| 代码/文件名（等宽） | Consolas |

### 10.3 占位符规则汇总

| 规则 | 说明 |
|------|------|
| 语法 | `{列名}` |
| 大小写 | 敏感 |
| 空白 | 自动 Trim 前后空格 |
| 缺失列 | 警告 + 替换为空字符串 |
| 空值 | 替换为空字符串 |
| 列名限制 | 禁止包含大括号 |

### 10.4 数据写入规则汇总

| 数据类型 | 处理方式 |
|----------|----------|
| DateTime | 保留类型 |
| 数值 | 保留类型 |
| 长数字（>11位） | 转文本 + 单引号前缀 |
| 空值 | 写入空字符串 |
| 公式 | 替换为计算值 |

---

**文档结束**
